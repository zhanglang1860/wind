<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="wind.list">
      <field name="name">wind list</field>
      <field name="model">wind.wind</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="wind.action_window">
      <field name="name">wind window</field>
      <field name="res_model">wind.wind</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="wind.action_server">
      <field name="name">wind server</field>
      <field name="model_id" ref="model_wind_wind"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="wind" id="wind.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="wind.menu_1" parent="wind.menu_root"/>
    <menuitem name="Menu 2" id="wind.menu_2" parent="wind.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="wind.menu_1_list" parent="wind.menu_1"
              action="wind.action_window"/>
    <menuitem name="Server to list" id="wind" parent="wind.menu_2"
              action="wind.action_server"/>
    -->
  </data>
</odoo>